node_modules/
*.db
*.db-journal
*.log
.env
.DS_Store
npm-debug.log*

# Docker (local-only)
backup/
docker-compose.yml
Dockerfile
.dockerignore

# macOS
.AppleDouble
.LSOverride

# Vercel
.vercel
.env*.local

# Sensitive config
backend/config/calendars.json
scraper/.booking-credentials.json
scraper/.booking-session.json

# Guest data exports (contain PII)
exports/*.csv
exports/*.xls
exports/*.xlsx
exports/*.json
!exports/README.md

# monitor-app deploy bundle needs its own copies (committed to its branch only).
# Files live in monitor-app/exports/ and MUST be shipped with the Vercel deploy.
!monitor-app/exports/*.csv
!monitor-app/exports/*.xls
!monitor-app/exports/*.xlsx

# Scraper data & debug (contain PII / credentials)
scraper/*.json
scraper/*.png
scraper/*.sql

# Debug
debug-screenshot.png
